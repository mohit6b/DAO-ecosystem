{
  "name": "hardhat-project",
  "scripts": {
    "test": "npx hardhat test",
    "deploy": "npx hardhat ignition deploy ignition/modules/DeployModule.js --network sepolia",
    "deploy-verify": "npx hardhat ignition deploy ignition/modules/DeployModule.js --network sepolia --verify"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "@nomicfoundation/hardhat-verify": "^2.0.13",
    "hardhat": "^2.23.0"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.3.0"
  }
}
